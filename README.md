# crud-notes
A simple CRUD web application built with Python using the Flask framework and SQLite. It allows users to create, view, edit, and delete notes through a minimal web interface using HTML templates.
